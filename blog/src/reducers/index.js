import { combineReducers } from 'redux'

export default combineReducers({
  replaceMe: () => 10
})
// can use dummy reducer in boilerplate
// export default combineReducers({
//   replaceMe: () => 10
// })
